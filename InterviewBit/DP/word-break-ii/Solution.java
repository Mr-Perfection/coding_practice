public class Solution {
    public static void word_break_helper(String s, ArrayList<String> sol, ArrayList<String> dict, int index)
    {

    	for (int i = index; i < s.length(); ++i)
    	{
    		s.substring(index , i);
    	}
        String str = new String(s.substring(i , j));
        if (dict.contains(str))
        {

        }
        
    }
	public ArrayList<String> wordBreak(String s, ArrayList<String> dict) {
	    
	}
}
